#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/vimals/JmixProjects/multitenancy-test/node_modules/.pnpm/nanobench@2.1.1/node_modules/nanobench/node_modules:/home/vimals/JmixProjects/multitenancy-test/node_modules/.pnpm/nanobench@2.1.1/node_modules:/home/vimals/JmixProjects/multitenancy-test/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/home/vimals/JmixProjects/multitenancy-test/node_modules/.pnpm/nanobench@2.1.1/node_modules/nanobench/node_modules:/home/vimals/JmixProjects/multitenancy-test/node_modules/.pnpm/nanobench@2.1.1/node_modules:/home/vimals/JmixProjects/multitenancy-test/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../nanobench/compare.js" "$@"
else
  exec node  "$basedir/../nanobench/compare.js" "$@"
fi
